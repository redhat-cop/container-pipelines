{{- $baseName := .Values.baseName -}}
{{- $friendlyBaseName := .Values.friendlyBaseName -}}
{{- range .Values.environments }}
---
kind: ProjectRequest
apiVersion: project.openshift.io/v1
metadata:
  name: "{{ $baseName }}-{{ . }}"
displayName: "{{ $friendlyBaseName }} - {{ . }}"
{{- end}}