# Default values for basic-dotnet-tekton.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

name: dotnet-ex
namespace: dotnet-tekton
readinessResponse: html
buildImage: image-registry.openshift-image-registry.svc:5000/openshift/dotnet:3.0
cliImage: image-registry.openshift-image-registry.svc:5000/dotnet-tekton/origin-cli:latest